# ═══════════════════════════════════════════
# Antigravity Portfolio — Application Config
# ═══════════════════════════════════════════

server.port=8080

# ── Active Profile ──
# Use 'dev' for H2 in-memory DB, 'prod' for PostgreSQL
spring.profiles.active=dev

# ── Application Name ──
spring.application.name=portfolio-backend
ot